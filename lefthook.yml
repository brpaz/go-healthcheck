pre-commit:
  commands:
    lint:
      glob: "*.go"
      run: task lint

    test:
      glob: "*.go"
      run: task test

    update-godoc:
      glob: "*.go"
      stage_fixed: true
      run: task godoc
